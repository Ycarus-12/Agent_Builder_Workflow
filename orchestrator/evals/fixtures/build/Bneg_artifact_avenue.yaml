case_id: B_neg_artifact_avenue
agent: build-agent
version: 1.0.0
description: NEGATIVE — code avenue but artifact uses staging_ref (wrong coupling)
expect_result: fail
replay_only: true
build_type: "code"
acceptance_criteria:
  - "Owner notified"
spec_context:
  data_sensitivity: "internal"
assertions: {}
recorded_output: |
  {
    "request_title": "Notify owner",
    "build_type": "code",
    "build_status": "complete",
    "artifact": {"repo_path": null, "commit_hash": null, "staging_ref": "staging/xyz", "config_guide": null},
    "artifact_summary": "Owner notifier.",
    "acceptance_criteria_map": [
      {"criterion": "Owner notified", "addressed_by": "notify.py:notify_owner()"}
    ],
    "qa_entrypoint": {"instructions": "Run it.", "run_command": "python notify.py", "dependencies": [], "environment_notes": null},
    "verification_status": "verifiable",
    "build_facts": ["compiles without errors"],
    "assumptions": [],
    "questions": []
  }
