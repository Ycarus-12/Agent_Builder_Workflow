case_id: Q_neg_manifest
agent: functional-qa
version: 1.0.0
description: NEGATIVE — evidence cites the manifest as proof (not independent)
expect_result: fail
replay_only: true
build_type: "code"
acceptance_criteria:
  - "Owner notified"
oracle:
  qa_status: "pass"
  verdicts: ["pass"]
assertions: {}
recorded_output: |
  {
    "request_title": "Notify owner",
    "build_type": "code",
    "qa_status": "pass",
    "verification_mode": "executed",
    "criteria_verdicts": [
      {"criterion": "Owner notified", "verdict": "pass", "method": "Reviewed the manifest.", "evidence": "Per the build facts, the notification is sent."}
    ],
    "findings": [],
    "failure_report": null,
    "validate_on_apply": [],
    "independence_note": "Reviewed."
  }
