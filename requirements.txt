# Posit Connect Cloud installs Python dependencies from this repo-root file.
# Runtime deps for the orchestrator app — pinned to match orchestrator/requirements.txt
# (minus test-only packages). Keep the two in sync.
fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
itsdangerous==2.2.0
jsonschema==4.23.0
PyYAML==6.0.2
httpx==0.27.2
