# Posit Connect Cloud installs Python dependencies from this repo-root file.
# The deployed content is the Shiny UI (intake_app.py / console_app.py); these are
# the runtime deps for the Shiny app + the reused orchestrator backend.
shiny==1.6.3
jsonschema==4.23.0
PyYAML==6.0.2
httpx==0.27.2
